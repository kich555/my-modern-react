import { useMutation } from 'react-query';
import { postTestRequest } from 'apis';
export default function Write() {
  // const { mutate } = useMutation(postTestRequest, { useErrorBoundary: true });

  return <div>tests</div>;
}
